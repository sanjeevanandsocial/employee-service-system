<!DOCTYPE html>
<html>
  <head>
    <title>Forgot Password - Employee Service System</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body style="margin: 0;">
    <div style="padding: 10px; background: #00226d; color: white; height: 40px; display: flex; align-items: center; font-size: 25px;">Forgot Password?</div>
    <div style="width: 500px; margin: 0 auto; margin-top: 50px;">
      <div class="form-card">
        <h1 class="form-title">Get a Verification Code</h1>
        <p style="text-align: center; color: #64748b;">Enter your registered email address to receive a verification code</p>
        
        <% if notice %>
          <div class="success-message">
            <%= notice %>
          </div>
        <% end %>

        <% if alert %>
          <div class="error-messages">
            <%= alert %>
          </div>
        <% end %>

        <%= form_with url: send_code_path, method: :post, local: true do |form| %>
          <div class="form-group">
            <%= form.email_field :email, class: "form-input", placeholder: "Enter your email address", required: true %>
          </div>

          <div class="btn-group">
            <%= form.submit "Send Verification Code", class: "btn-primary" %>
            <%= link_to "Back to Login", new_user_session_path, class: "btn-secondary" %>
          </div>
        <% end %>
      </div>
    </div>
  </body>
</html>